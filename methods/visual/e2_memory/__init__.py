"""Memory-form baselines for formal Visual WCM state-return experiments.

The package deliberately separates public RGB feature extraction and method
models from the train/validation-only supervision code in :mod:`training`.
"""
